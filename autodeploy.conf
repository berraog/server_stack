# <repo path>  <compose service>  [branch, default main]
# One line per app that should redeploy itself on a push. ~/stack is
# deliberately absent: infrastructure changes restart real services, so pull
# those by hand (README §12).
/home/bjorngreen/git/matte-vm        app
